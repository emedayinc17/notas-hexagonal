apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: sga
  labels:
    app: sga-frontend
data:
  # URL base del backend visto desde el navegador
  FRONTEND_API_URL: "http://sga.emeday.inc"

  # Este config.js se monta como archivo estático en el contenedor
  config.js: |
    window.APP_CONFIG = {
      API_BASE: "http://sga.emeday.inc",
    };
