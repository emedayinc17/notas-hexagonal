resources:
  - ../../base

# Use the newer 'patches' with explicit target so kustomize can find the Deployment
patches:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: iam-service
      namespace: sga
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
